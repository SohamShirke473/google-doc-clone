const authConfig = {
    providers: [
        {
            domain: "https://thorough-blowfish-72.clerk.accounts.dev",
            applicationID: "convex"
        }
    ]
};

export default authConfig;
